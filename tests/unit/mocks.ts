/*import {TextEditorSubject, PwaRouteObserver} from "@/components/textEditorLibrary"

export class SubjectMock extends TextEditorSubject {
    constructor(observerList: PwaRouteObserver[], routes: string[]){
        super(observerList,routes);
    }
}

export class ObserverMock implements PwaRouteObserver {
    private route: string[];
    constructor(newRoute: string[]){
        this.route= newRoute;
    }
    updateRouteList(routePathName: string) {
        this.route.push(routePathName);
    }


    public getRoute(){
        return this.route;
    }
    
}*/