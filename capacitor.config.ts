import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Text Editor - PWA',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
